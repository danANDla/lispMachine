(defmacro when (condition body) '(if ,condition (progn ,body)))
(setq limit 0)
(scan limit)
(setq a 2)
(setq b 1)
(print b)
(loop
(setq temp a)
(setq a (+ a b))
(print a)
(setq b temp)
(when (> a limit) (return a)))
